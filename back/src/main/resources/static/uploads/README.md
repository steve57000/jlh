# Dossier d'uploads statiques (démo)

Les fichiers présents ici sont servis directement par Spring Boot via `/uploads/**`.
Ce mécanisme permet d'afficher des icônes/images/documents en mode démo, sans
stockage persistant externe.
